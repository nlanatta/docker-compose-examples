## Compose Projects
# Compose Kafka
# Compose Postgres
# Compose Redis
# Compose MongoDB

### RUN INSIDE Containers:
docker container exec -ti [my_container_id] bash

### GET container ID
docker container ls